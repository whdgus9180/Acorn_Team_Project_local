<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="info">

<select id="getWritingList" parameterType="usersInfoDto" resultType="usersInfoDto">
		SELECT *
		FROM
			(SELECT result1.*, ROWNUM AS rnum
			FROM
				(SELECT comu_num,writer,title,regdate,group_num
				FROM community_board_tb
				WHERE writer = #{writer}
				ORDER BY comu_num DESC) result1)
		<![CDATA[ 
		WHERE rnum >= #{startRowNum} AND rnum <= #{endRowNum}
		]]>
	</select>
	
	<select id="getCount" parameterType="usersInfoDto" resultType="int">
		SELECT NVL(MAX(ROWNUM), 0)
		FROM community_board_tb
		WHERE writer = #{writer}
	</select>

</mapper>